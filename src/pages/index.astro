---
import Document from '../Document.astro';
import { EventLayout } from '../components/layouts/EventLayout';
import { getEvent } from '../services/contentful';

const [event, pastEvents] = await getEvent({ includePastEvents: true });
---

<Document>
  <EventLayout client:load event={event} pastEvents={pastEvents} />
</Document>
